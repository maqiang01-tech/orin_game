# A | 资源生成 - Gate 0 Cut Plan

This staging-only package plans the approved four UI shell families for `battle_sandbox_g0_ui_shells`.

It contains no PNG candidates, Manifest, production changes, or Godot edits. The future candidate set is exactly 14 PNG files: four noninteractive panel shells, two 140x70 formation-slot bases, seven geometry-invariant skill-button states, and one vertically stretchable battle-log shell.

All labels, values, portraits, semantic icons, badges, targeting lines, cooldown values, and audit log entries remain runtime controls. Production is blocked pending `C_ASSET:plan_review` and a new S production dispatch.
